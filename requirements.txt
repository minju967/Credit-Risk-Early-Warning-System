# =============================================================================
# AI Campus 금융·핀테크 AI 강의 통합 requirements.txt
# 기준 환경: Windows 10/11 + Miniforge + Python 3.11 + aicam
# 작성 기준: Pandas 3.x / Jupyter Notebook 7.6.2
# 설치: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# 01. Jupyter Notebook 개발환경
# -----------------------------------------------------------------------------
notebook==7.6.2
ipykernel>=6.29,<7
ipywidgets>=8.1,<9

# -----------------------------------------------------------------------------
# 02. 데이터 분석 / 통계
# Pandas 3.0.5를 기준으로 사용합니다.
# NumPy 2.1.3은 TensorFlow 2.20 계열까지 같은 Python 3.11 환경에서
# 함께 사용하기 위한 보수적인 호환 버전입니다.
# -----------------------------------------------------------------------------
pandas==3.0.5
numpy==2.1.3
scipy==1.14.1
pyarrow>=18,<25
matplotlib>=3.9,<3.11
seaborn==0.13.2
plotly>=5.24,<6
koreanize-matplotlib
openpyxl>=3.1,<4
statsmodels==0.14.6

# -----------------------------------------------------------------------------
# 03. 환경변수 / 설정 / 진행상태
# -----------------------------------------------------------------------------
python-dotenv>=1.0,<2
PyYAML>=6.0,<7
tqdm>=4.66,<5

# -----------------------------------------------------------------------------
# 04. MySQL / SQL
# -----------------------------------------------------------------------------
SQLAlchemy>=2.0,<2.1
PyMySQL>=1.1,<2
cryptography>=43,<47

# -----------------------------------------------------------------------------
# 05. Machine Learning / FDS / CSS
# 아래 4개 패키지는 기존 FDS 직렬화 모델 재현성을 위해 버전을 고정합니다.
# -----------------------------------------------------------------------------
scikit-learn==1.7.2
joblib==1.5.1
xgboost==3.2.0
lightgbm==4.7.0

imbalanced-learn==0.14.2
shap>=0.46,<0.51
lime==0.2.0.1
networkx>=3.2,<4
gensim==4.4.0
optbinning==0.21.0
toad==0.1.7
evidently==0.7.21


# -----------------------------------------------------------------------------
# 07. Streamlit MVP
# Streamlit의 PyArrow 호환 범위를 고려해 위에서 pyarrow<25로 제한합니다.
# -----------------------------------------------------------------------------
streamlit==1.63.0

# -----------------------------------------------------------------------------
# 08. FastAPI 서비스 / 운영
# -----------------------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]>=0.35,<1
gunicorn>=23,<24
pydantic>=2.11,<3

# -----------------------------------------------------------------------------
# 09. HTTP / HTML / 공시 수집
# -----------------------------------------------------------------------------
requests>=2.32,<3
beautifulsoup4>=4.13,<5
lxml>=5,<7
playwright>=1.50,<2

# -----------------------------------------------------------------------------
# 10. LLM / RAG / Embedding / FAISS
# 기존 강의 노트의 Transformers API 호환성을 위해 4.x 범위를 사용합니다.
# -----------------------------------------------------------------------------
torch>=2.6,<3
transformers>=4.41,<5
sentence-transformers>=3,<6
huggingface-hub>=0.30,<1
faiss-cpu>=1.8,<2

# =============================================================================
# 설치 후 확인
#   pip check
#
# Playwright를 실제 사용하는 수업 전 한 번 실행
#   playwright install chromium
#
# Jupyter Kernel 등록
#   python -m ipykernel install --user --name aicam --display-name "Python (aicam)"
# =============================================================================
